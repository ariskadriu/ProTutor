{
    "id": "py_ml_kmeans_en",
    "title": "Python ML: K-means Clustering",
    "description": "K-means is an unsupervised learning algorithm that group data into K number of clusters. The algorithm works iteratively to assign each data point to one of K groups based on the features that are provided. It is a powerful tool for finding patterns in unlabeled data.",
    "example": "# K-means Implementation\nimport matplotlib.pyplot as plt\nfrom sklearn.cluster import KMeans\n\nx = [4, 5, 10, 4, 3, 11, 14 , 6, 10, 12]\ny = [21, 19, 24, 17, 16, 25, 24, 22, 21, 21]\ndata = list(zip(x, y))\n\nkmeans = KMeans(n_clusters=2)\nkmeans.fit(data)\n\nplt.scatter(x, y, c=kmeans.labels_)\nplt.show()",
    "challenges": [
        {
            "title": "K-clusters",
            "exercise": "Print 'K-means is unsupervised learning' to confirm you understand the type of algorithm.",
            "expectedOutput": "K-means is unsupervised learning",
            "solution": "print('K-means is unsupervised learning')",
            "difficulty": "Hard",
            "requiredKeywords": ["unsupervised"]
        }
    ]
}
