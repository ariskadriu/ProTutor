{
    "id": "py_dsa_en",
    "title": "Python DSA",
    "description": "Data Structures and Algorithms are the fundamentals of computer science. Python provides built-in structures like lists and dicts.",
    "example": "def binary_search(arr, low, high, x):\n  if high >= low:\n    mid = (high + low) // 2\n    if arr[mid] == x: return mid\n    elif arr[mid] > x: return binary_search(arr, low, mid - 1, x)\n    else: return binary_search(arr, mid + 1, high, x)\n  return -1",
    "challenges": [
        {
            "title": "Linear Search",
            "exercise": "Write a function 'search(arr, n)' that returns the index of 'n' in 'arr' using a for loop. Test with [1,2,3], 2.",
            "expectedOutput": "1",
            "solution": "def search(arr, n):\n  for i in range(len(arr)):\n    if arr[i] == n: return i\n  return -1\n\nprint(search([1, 2, 3], 2))",
            "difficulty": "Hard",
            "requiredKeywords": ["def search", "for", "range", "return"]
        }
    ]
}
