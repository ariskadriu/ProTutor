{
    "id": "cs_enums_en",
    "title": "C# Enums",
    "description": "An enum is a special 'class' that represents a group of constants (unchangeable variables). Use enums when you have values that you know aren't going to change, like month names, days, colors, deck of cards, etc.",
    "example": "// Enum Definition\nenum Level {\n  Low,\n  Medium,\n  High\n}\n\n// Usage inside a class\nLevel myVar = Level.Medium;\nConsole.WriteLine(myVar);",
    "challenges": [
        {
            "title": "Enum Value",
            "exercise": "Create enum 'TrafficLight' with Red, Yellow, Green. Print 'TrafficLight.Red'.",
            "expectedOutput": "Red",
            "solution": "enum TrafficLight { Red, Yellow, Green }\nstatic void Main() {\n  Console.WriteLine(TrafficLight.Red);\n}",
            "difficulty": "Medium",
            "requiredKeywords": ["enum", "Red"]
        }
    ]
}
