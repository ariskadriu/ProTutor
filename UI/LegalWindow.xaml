<Window x:Class="ProgrammingTutor.UI.LegalWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Legal Information" Height="500" Width="600"
        WindowStartupLocation="CenterOwner"
        Background="{StaticResource BackgroundBrush}"
        Foreground="{StaticResource TextBrush}">
    
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <TextBlock x:Name="LegalHeader" Text="Legal Information" FontSize="24" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>
        
        <Border Grid.Row="1" Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="15">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock x:Name="LegalText" TextWrapping="Wrap" Foreground="{StaticResource TextBrush}" LineHeight="22" FontSize="13"/>
            </ScrollViewer>
        </Border>
        
        <Button Grid.Row="2" Content="Close" Click="Close_Click" Style="{StaticResource ModernButton}" HorizontalAlignment="Right" Margin="0,20,0,0" Width="100"/>
    </Grid>
</Window>
